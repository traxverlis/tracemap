import { IdentifierManagerPage } from './IdentifierManagerPage'

export function IdentifiersPage(): JSX.Element {
  return (
    <IdentifierManagerPage
      title="All identifiers"
      description="Manage the full identifier inventory for the active identity across email, phone, username, address, and domain records."
    />
  )
}
